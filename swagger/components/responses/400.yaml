description: ID is not valid
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 400
        message:
          type: string
          example: ID is not valid
        data:
          type: object
          required:
            - message
          properties:
            message:
              type: string
              example: 'ID is not valid'
